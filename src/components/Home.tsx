import { Typography } from "@material-ui/core";
import React from "react";

const Home = () => {
  return (
    <Typography variant="h2">Welcome to the Habit Tracker app!</Typography>
  );
};

export default Home;
